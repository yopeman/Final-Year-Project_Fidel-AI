type QuizResults {
  id: ID!
  enrollmentId: ID!
  quizId: ID!
  score: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
  isDeleted: Boolean!
  deletedAt: DateTime
  # Relationships
  enrollment: BatchEnrollment!
  quiz: Quiz!
}
